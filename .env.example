# ルート .env — copy to .env (gitignored)
# 詳細: docs/deploy-fork.md#env-files

# --- Docker Compose (make up / make up-tunnel) ---
# 1. https://one.dash.cloudflare.com/ → Zero Trust → Networks → Tunnels
# 2. Create a tunnel → cloudflared → copy token
TUNNEL_TOKEN=""

# --- E2E smoke (optional) ---
# RADIO_E2E_WORKERS_URL=https://radio.<account>.workers.dev
# RADIO_E2E_PROD_URL=https://your-domain.com
